kubectl -n gitlab get pod gitlab-webservice-default-7dc54c856f-k8kph -o json \
| jq '.status.containerStatuses[] | {name:.name, lastTerminated:.lastState.terminated, waiting:.state.waiting}'

kubectl -n argocd get secret repo-gitlab-iot-http -o yaml
or 
kubectl -n argocd get secret repo-gitlab-iot-http -o jsonpath='{.data}' | jq -r 'to_entries[] | "\(.key): \(.value | @base64d)"'


Prove the eviction reason
# See recent cluster-wide reasons
kubectl get events -A --sort-by=.lastTimestamp | tail -n 80

# Look at one evicted pod to see the exact cause line
kubectl -n argocd describe pod argocd-server-565f766db-njn2h | sed -n '/Events:/,$p'

# Check node conditions + allocatable
kubectl get nodes -o wide
kubectl describe node k3d-demo-server-0
kubectl describe node <your-node-name> | egrep -i 'Pressure|Allocatable|Capacity|evict|imagefs|nodefs'

